<!DOCTYPE html>
<html>
<link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">

<p id="notice" class="w3-panel"><%= notice %></p>

<div class="w3-panel w3-pale-red">
<p></p>
<table class="w3-table-all">

<thead>
  <tr>
    <th><h1 class="w3-wide"><i class="material-icons">shopping_cart</i> CART</h1></th>
    <th></th>
  </tr>
</thead>
<tbody>
  
<% @cart.line_items.each do |item| %>  
<tr>
  <td><%= item.quantity %> &times; <%= item.product.title %></td>
  <td><%= number_to_currency(item.total_price) %></td>
</tr>
<% end %>
    <tr>
    <td>Total Order Value </td>
    <td><%= number_to_currency(@cart.total_price) %></td>
    </tr>
<% if session[:user_id] %>
<tr>
<td>
<%= link_to 'Edit', edit_cart_path(@cart) ,class:"w3-button w3-light-gray w3-border" %> 
</td>
<td>
<%= link_to 'Back', carts_path , class:"w3-button w3-light-gray w3-border" %>
</td>
</tr>
<% end %>
</tbody>
</table>
  <br>
<div><%= button_to 'Empty cart', @cart, method: :delete, data: { confirm: 'Are you sure?' }, class:"w3-button w3-light-gray w3-border" %></div>
  <div><%= link_to 'Keep Shopping', store_index_url , class:"w3-button w3-light-gray w3-border" %></div>
  <div><%= button_to "Checkout", new_order_path, method: :get, data: { confirm: 'Proceed to Check Out?' }, class:"w3-button w3-light-gray w3-border"  %></div>
  
  <p></p>
</div>
  
</html>